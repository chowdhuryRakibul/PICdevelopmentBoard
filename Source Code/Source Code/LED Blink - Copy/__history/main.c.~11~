//#include <main.h>

#include <18F4550.h>
#fuses NOMCLR INTRC_IO
#use delay(clock=8000000)
void main()
{
   port_d_pullups(TRUE);
   while(TRUE)
   {
      output_high(PIN_D0);
      delay_ms(1000);
      output_low(PIN_D0);
      delay_ms(1000);
   }

}
